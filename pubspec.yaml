name: platech_tap_to_pay
description: Plugin Flutter privado para o SDK Android Platech TapToPay.
version: 1.0.3
publish_to: none
repository: https://github.com/platech-api/platech-package-public

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: site.platech.taptopay
        pluginClass: PlatechTapToPayFlutterPlugin
